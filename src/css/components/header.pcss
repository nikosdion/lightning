/*
 * @package     Lightning
 *
 * @copyright   Copyright (C) 2020-2021 Nicholas K. Dionysopoulos. All rights reserved.
 * @license     GNU General Public License version 2 or later; see LICENSE.txt
 *
 * This template is a derivative work of the Lightning template which is
 * Copyright (C) 2020 JoomJunk.
 */

/* Header */

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  height: var(--navbar-height);
  margin-bottom: 1.5rem;
  padding: .5rem 1.5rem;
  box-shadow: 0 2px 8px 0 rgba(0, 0, 0, .1);
  background-color: var(--hiq-header-background-color);
  line-height: calc(var(--navbar-height) - 1rem);
  color: var(--hiq-header-text-color);

  a {
    color: var(--hiq-header-link-color);
  }

  &.has-banner {
    margin-bottom: 0;
  }

  ul.mod-menu + div {
      margin-left: 1em;
  }
}
